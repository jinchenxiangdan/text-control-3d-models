
using UnityEditor;
using UnityEngine;

public class StartupScript
{
    
    // Start is called before the first frame update
    void Startup()
    {
        // Debug.log("Up and run");
        // print("Starting up");

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
