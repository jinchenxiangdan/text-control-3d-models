using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UpdateFeatures : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // List<GameObject> myObjects = GameObject.FindGameObjectsWithTag("3d_object");
        // foreach (object obj in myObjects)
        //     print(obj + ".sd");
        print("game start!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
